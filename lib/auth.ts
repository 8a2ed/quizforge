// NextAuth config — not used directly (custom JWT session via jose instead),
// but kept for future OAuth provider support.
// The app uses a custom Telegram Login Widget flow in /api/auth/telegram.
export {};
